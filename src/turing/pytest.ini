[pytest]
DJANGO_SETTINGS_MODULE = turing.settings
python_files = tests.py test_*.py *_tests.py
python_classes = Test* *Tests *TestCase
python_functions = test_*
addopts = 
    --reuse-db
    --no-migrations
    -v
    --tb=short
testpaths = chatbot courses users teachers
